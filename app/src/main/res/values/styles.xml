<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ButtonInShop" parent="Base.AlertDialog.AppCompat.Light">
        <item name="android:textColor">@color/btnText</item>
        <item name="android:onClick">onClick</item>
        <item name="android:textSize">28sp</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:padding">5dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:background">@drawable/button_enable</item>
    </style>

    <style name="ButtonStart" parent="Base.AlertDialog.AppCompat.Light">
        <item name="android:textColor">@color/btnText</item>
        <item name="android:onClick">onClick</item>
        <item name="android:background">@color/colorBtnShop</item>
        <item name="android:textSize">28sp</item>
        <item name="android:width">180dp</item>
        <item name="android:padding">5dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>

    </style>

</resources>
